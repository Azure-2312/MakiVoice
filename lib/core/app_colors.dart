import 'package:flutter/material.dart';

class AppColors {
  // Paleta de colores: Azul Petróleo + Naranja Ingeniería ("Ingeniería aplicada para cambiar vidas")
  static const background   = Color(0xFFFAE0A5);  // Arena (Fondo principal de la app)
  static const surface      = Color(0x99FFFFFF);  // Blanco translúcido más transparente (Efecto ventana)
  static const surfaceLight = Color(0x80EADBBE);  // Beige translúcido suave (Bordes y divisores)
  static const accent       = Color(0xFF073245);  // Azul Petróleo (Encabezados, títulos, acciones primarias)
  static const accentDim    = Color(0xFFB35E24);  // Naranja (Sensores, innovación, racha)
  static const textPrimary  = Color(0xFF4A6596);  // Gris Grafito (Textos principales)
  static const textSecond   = Color(0xFF5B7BAE);  // Gris Grafito medio (Textos secundarios)
  static const error        = Color(0xFFEF4444);  // Rojo (Errores y alertas)
  static const connected    = Color(0xFFB35E24);  // Naranja (Conexiones, sensores)
  static const disconnected = Color(0xFF9CA3AF);  // Gris neutro (Estado BLE Desconectado)
}
